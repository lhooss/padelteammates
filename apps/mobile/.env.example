# Optionnel. Par defaut, l'app joint l'API sur l'IP du PC qui lance `expo start`, port 3001
# (telephone et PC sur le meme Wi-Fi). A definir pour viser une autre API :
# EXPO_PUBLIC_API_URL=https://api.exemple.ma
